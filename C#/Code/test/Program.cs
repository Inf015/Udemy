using System;

namespace Coding.Exercise
{
    public class Method
    {
        // Place for your methods
        public static void LowUpper(string LowUpper)
        {
            Console.WriteLine(properString.ToUpper(s));
            Console.WriteLine(properString.ToLower(s));
        }
        
        public static void Run()
        {
            // We encourage you to test your code with different strings,
            // but don't forget to put the default string back at the end of your testing.
            string s = "HeY ThErE !";
            
            /// Change nothing down here 
            s = LowUpper(s);
            Console.WriteLine(s);
            Count(s);
            
        }
    }
}
